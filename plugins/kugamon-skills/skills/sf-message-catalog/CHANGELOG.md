# Changelog

## 1.0.0 (2026-04-17)

- Initial release
- Apex class and trigger message extraction (`addError`, `ApexPages.addMessage`, thrown exceptions)
- LWC message extraction (`ShowToastEvent`, `setCustomValidity`, `.catch()` handlers, custom error components)
- Aura component message extraction (`force:showToast`, `ServerActionService`, `c:Alert`/`c:Toast`)
- Visualforce page message extraction (`apex:pageMessages`, `apex:pageMessage`, JS `alert()`/`confirm()`)
- Visualforce component message extraction (inline validation, watermarks, conditional panels)
- Parallel extraction via subagents
- Professional landscape .docx output with executive summary, severity legend, and color-coded tables
- Monthly scheduling support
